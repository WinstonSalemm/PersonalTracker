[build]
builder = "NIXPACKS"
buildCommand = "npm run prisma:generate && npm run build"

[deploy]
startCommand = "npm run prisma:seed && npm start"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
