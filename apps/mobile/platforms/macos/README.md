# macOS wiring template

Add an `NSStatusItem` menu bar host and a user-approved global shortcut through `NSEvent`/Shortcut integration around the generated Flutter macOS runner. The action should open the same Quick Capture route. Ask for accessibility permission only when the user enables the global shortcut.
